# Environment variables for the Baalvion Jobs Portal

# The full public URL of your deployment
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# The base URL for your backend API (if separate)
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Firebase Project Configuration (if using Firebase)
FIREBASE_PROJECT_ID=
FIREBASE_API_KEY=
